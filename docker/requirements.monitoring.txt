# Monitoring dashboard service dependencies
# Pin Evidently to 0.7.x — the API changed materially in 0.5.
# monitor.py is written against the 0.7.x legacy Report/DataDriftPreset interface.
evidently==0.7.21

# FastAPI + server
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4

# GCS access
google-cloud-storage==2.16.0
google-cloud-bigquery==3.20.0

# Data / inference deps (needed by monitor.py which imports from pipelines.train)
pandas==2.2.2
pyarrow==17.0.0
scikit-learn==1.4.2
xgboost==2.0.3
mlflow==2.17.2
joblib==1.4.0
